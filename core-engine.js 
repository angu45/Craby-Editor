const dictionary = {
    html: ['div', 'span', 'h1', 'p', 'a', 'img', 'button', 'input', 'section', 'footer', 'header', 'nav'],
    css: ['color', 'background', 'margin', 'padding', 'display', 'flex', 'grid', 'position', 'width', 'height'],
    js: ['console.log', 'document.getElementById', 'function', 'const', 'let', 'addEventListener']
};

function toggleSettings() {
    document.getElementById('settingsPanel').classList.toggle('open');
}

function runCode() {
    document.getElementById('preview-overlay').style.display = 'flex';
    const h = document.getElementById('html-code')?.value || '';
    const c = `<style>${document.getElementById('css-code')?.value || ''}</style>`;
    const j = `<script>${document.getElementById('js-code')?.value || ''}<\/script>`;
    
    const out = document.getElementById('output').contentWindow.document;
    out.open(); out.write(h + c + j); out.close();
}

function closePreview() { document.getElementById('preview-overlay').style.display = 'none'; }

// Initialization
window.onload = () => {
    // Default 2 Pages
    addFileToUI("index.html", "html", "<h1>Craby Editor</h1>");
    addFileToUI("style.css", "css", "h1 { color: orange; }");
    
    document.getElementById('theme-sel').value = 'dark';
    document.getElementById('font-size-bar').value = 16;
    updateThemeAndFont();
};

// Input Listeners & Suggestions (Shortened)
function attachInputListeners(txt) {
    txt.addEventListener('input', () => {
        // तुझी जुनी Auto-complete सिस्टिम इथे टाकू शकतोस
    });
}
